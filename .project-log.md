# Project Log

Internal project history and task tracker for Claude Code sessions.

> **Internal only.** This file is committed to GitHub but is **not deployed**.
> It lives at the project root, outside `src/` and `public/`, so neither
> Astro nor Cloudflare Pages can serve it. Keep it that way.

> **Before starting any work**, read this file first.
> **Before ending any work**, update this file with completed work,
> unfinished work, and next actions.
> **Keep entries short, useful, and factual.**

---

## Current Priorities

- Per-client launch checklist (see `.claude/skills/performance.md` Section 12)

---

## Unfinished / To Do

- [ ] **Pending invitations awaiting acceptance:** hmdg-larry (admin), hmdgai (admin) — re-invited to grant admin (no repo access until they accept); hmdg-Dave (write).
- [ ] **Uncommitted:** project-rules.md access model now "3 Admins (felmerald-hmdg, hmdg-larry, hmdgai) + 10 Developers" + log updates — awaiting Fel's push command.
- [ ] **Per-client deploy assets** (only relevant when actually deploying a client clone):
  - [ ] Drop `public/images/contact-hero.webp` (1920×800, ~80 KB)
  - [ ] Drop `public/og-image.webp` (1200×630)
  - [ ] Set `site:` in `astro.config.mjs` to the production domain
  - [ ] Set the four `PUBLIC_*` env vars on Cloudflare Pages → Build scope
  - [ ] Click "Retry deployment" after env vars are saved
- [ ] **At launch** — flip `noindex` default in `BaseLayout.astro` from `true` → `false` so non-thank-you pages become indexable.
- [ ] **Post-domain** — migrate from deferred GTM to Cloudflare Zaraz (see `.claude/skills/performance.md` Section 10). Eliminates 150–220 KB of browser-side JS.
- [ ] **Optional** — assess `npm audit` results from the Astro 6.1.9 bump (6 moderate transitive vulnerabilities, likely dev-only via fluent-ffmpeg).

---

## Blocked

- _(nothing currently)_

---

## Change Log

### 2026-06-10 (seo-reviewer upgraded to senior multi-domain specialist)

**Status:** completed (local; awaiting push)
**Task summary:** Rewrote `.claude/agents/seo-reviewer.md` into a senior SEO / Local SEO / technical SEO / schema / GEO+AISEO specialist scoped to the Astro+Tailwind+TS+Cloudflare stack and UK healthcare clinics (physio, chiro, podiatry, private health, specialist providers). Added: Algorithm Awareness (check current best practice, no outdated tactics, YMYL framing); Stack Integration section grounding every rule in real files (BaseLayout schema/metadata props, Astro.site canonical, src/data/site.ts NAP source, normalize.ts schema builders, auto-sitemap, static-render requirement, noindex launch flip); nine capability sections (metadata, local SEO, schema types incl. honest-only/no-fake-reviews rule, GEO/AISEO, keyword strategy, technical+stack compatibility); 13-step required review workflow; Delegation section (frontend-builder, performance-reviewer, a11y-reviewer, marketing/conversion); structured 9-point Output. Model stays claude-sonnet-4-6. Refined per Fel: metadata section rebuilt around search-intent + pixel-width (50-60 chars / 580-600px titles, 110-120 char mobile-critical description window) over raw character counts, multi-engine targeting (Google/AIO/Bing/ChatGPT/Perplexity/Gemini/Claude), local title formats, a metadata-validation checklist, AI-extraction entity list, and CTR/pixel reporting in Output.
**Files changed:**
- `.claude/agents/seo-reviewer.md`
**Next action:** push on Fel's command.

---

### 2026-06-10 (Access model: Owner + Developer mode)

**Status:** completed (rules edit local; awaiting push command)
**Task summary:** Fel decided on the Owner + Developer access model. Repo aligned: the 4 collaborators showing admin (dannis-HMDG, princesshmdg, hmdg-antonio, hmdg-junemark) downgraded to Write — all 12 active collaborators now uniform Write (developer mode), felmerald-hmdg sole owner/admin; hmdg-Dave pending invite set to Write. (GitHub allows lowering to write on personal repos; raising to admin is what gets ignored.) project-rules.md GitHub section rewritten: "Invite as Administrators" → Owner + Developer model with the personal-account limitation documented.
**Files changed:**
- `.claude/rules/project-rules.md`
**Next action:** push on Fel's command.

---

### 2026-06-10 (Push to GitHub + collaborator audit)

**Status:** completed
**Task summary:** Committed and pushed all session work (42 files, +2008/−319) as `5531374` on user command. Initial push accidentally created a remote `main` branch; corrected by fast-forwarding `master` (the default branch), deleting the stray remote `main`, and pointing local `main` to track `origin/master`. Collaborator audit vs project-rules: all 13 required accounts present (12 active + `hmdg-Dave` pending invite). Attempted to raise 7 "write" collaborators to admin — GitHub returns 204 but silently ignores `permission=admin` on personal-account repos (felmerald-hmdg is a User, not an Organization). hmdg-Dave's pending invitation was successfully updated to admin permissions.
**Files changed:** none (git + GitHub API operations)
**Next action:** Fel to decide whether to transfer the repo to an HMDG organization to enable true admin roles + branch protection.

---

### 2026-06-10 (Master project Rules — "Hey Claude, read our Rules")

**Status:** completed (changes locally; awaiting push) — build verified
**Task summary:** Codified Fel's master Rules document. Created `.claude/rules/project-rules.md` (canonical Rules: setup procedure with mandatory project-name stop, review-before-work requirement, section naming + numbering + padding system, code standards, GitHub repo setup with 13 admin collaborators, colour/typography reporting, favicon procedure, image formats avif-primary, hero video ≤15s/≤5MB, full QA gate). Implemented the section system in `global.css`: numbering classes `firstsection`–`tenthsection` + `herosection` now DEFINED in @layer components (were referenced but undefined); `--spacing-section-y` 70px→80px mobile per Rules; new `--spacing-hero-top` 180px token. frontend-builder upgraded to enforcement agent (Standards Enforcement Mandate, mandatory section naming/numbering/padding blocks, strict TypeScript rules, Completion Gate). CLAUDE.md: trigger registered in Rule References, new Sections non-negotiable line, images line now avif-primary, video line gains 15s/5MB hard ceiling (default encode spec unchanged). Memory feedback_image_formats.md: prefer .avif for new assets.
**Files changed:**
- `.claude/rules/project-rules.md` (new)
- `src/styles/global.css` — numbering classes defined; mobile section padding 70→80px (visual change, per Rules); hero-top token
- `.claude/agents/frontend-builder.md`
- `CLAUDE.md`
- `.claude/memory/feedback_image_formats.md`
**Notes:** `npm run build` clean. The 70→80px mobile change affects contact-strip + legal pages on <1200px viewports — mandated by the Rules' padding table.
**Next action:** Commit + push when user gives the command.

---

### 2026-06-10 (Rules + memory + CLAUDE.md sync with upgraded agents)

**Status:** completed (changes locally; awaiting push)
**Task summary:** Audited `.claude/rules/`, `.claude/memory/`, and CLAUDE.md against the upgraded agent fleet; fixed every contradiction. Pipelines: visual-qa-reviewer was never wired into agent-delegation/agent-workflow — now in Tier 4/5 pipelines, Wave 0.5, the announcement example, and the prompt template (step 4.5); Tier 5 count corrected 11→12 core agents; new "Domain Specialist Agents" section registers sanity-developer, sanity-reviewer, resent-email. Image formats: frontend.md/performance.md/quality.md now say `.webp` OR `.avif` (both accepted) — and the dangerous stale memory `feedback_image_formats.md` ("WebP only, no AVIF") was rewritten to match the standing override. WCAG: quality.md now states WCAG 2.2 AA with the new criteria. LCP: performance.md gained mandatory LCP element rules + Lighthouse weights; design.md hero rules gained the LCP budget line; frontend.md image pattern gained fetchpriority/preload. Stale memories: continuation-protocol.md (Netlify-era, v1.5.0) rewritten as SUPERSEDED pointer to .project-log.md with a wrong→current corrections table; cookieconsent.md got a HISTORICAL header (implementation complete, Cloudflare not Netlify); project_agent_workflow.md + project_agent_delegation.md + feedback_agent_announcement.md agent counts/sequences corrected; MEMORY.md index updated. CLAUDE.md: project structure now shows src/data/ + src/lib/content/ (Sanity-ready layer); SANITY_READY.md added to the docs list.
**Files changed:**
- `.claude/rules/agent-delegation.md`, `agent-workflow.md`, `frontend.md`, `performance.md`, `quality.md`, `design.md`
- `.claude/memory/feedback_image_formats.md` (rewritten), `continuation-protocol.md` (rewritten), `cookieconsent.md`, `project_agent_workflow.md`, `project_agent_delegation.md`, `feedback_agent_announcement.md`, `MEMORY.md`
- `CLAUDE.md`
**Next action:** Commit + push when user gives the command.

---

### 2026-06-10 (Sanity-ready content layer — no Sanity integration)

**Status:** completed (changes locally; awaiting push) — build verified
**Task summary:** Made the template Sanity-ready WITHOUT installing/integrating Sanity. Created a typed local data layer that future GROQ queries will replace 1:1. Components are now render-only. Fixed a real bug en route: Footer and contact.astro carried DIFFERENT placeholder clinic details (address/phone/email/hours disagreed) — now both render from one source, so footer hours/address/phone changed to the contact-page values (placeholder data only). Zero new client-side JS; all pages remain fully static; LCP path untouched.
**Files changed:**
- `src/lib/content/types.ts` (new) — content shape contracts mapping to future Sanity schemas (SiteSettings, NavLink, Service, TeamMember, Faq, HeroContent, ImageRef with mandatory alt/width/height, OpeningHours)
- `src/lib/content/normalize.ts` (new) — formatOpeningHours (grouped display), buildOpeningHoursSpec + buildMedicalClinicJsonLd (lifted from contact.astro)
- `src/data/site.ts` (new) — single source of truth for clinic identity (PUBLIC_SITE_NAME env honoured)
- `src/data/navigation.ts` (new) — headerNav, footerQuickLinks, footerLegalLinks
- `src/data/services.ts` (new) — services list (footer column consumer)
- `src/components/Header.astro` — renders nav/name/CTA from data; generic linkActive helper (hash links never active)
- `src/components/Footer.astro` — all four columns + legal links render from data; hours via formatOpeningHours
- `src/pages/contact.astro` — clinic consts now assigned from site.ts (same names; 1,000-line markup untouched); JSON-LD via normalize.ts
- `SANITY_READY.md` (new) — current workflow, schema mapping table, future query location (src/lib/sanity/), performance rules (build-time fetch only, webhook→deploy-hook rebuilds, no client-side CMS fetching), optional future env vars, integration checklist
**Notes:** `npm run build` clean — 9 routes prerendered. Built HTML verified: unified address/hours/email across contact + footer, MedicalClinic JSON-LD intact, footer is-active intact. reviews.ts was already the reference pattern and is unchanged. No content collections created (no markdown content yet — documented as future blog path).
**Next action:** Commit + push when user gives the command. Sanity integration itself is a future task (sanity-developer leads; checklist in SANITY_READY.md §6).

---

### 2026-06-10 (New agent — resent-email migration lead)

**Status:** completed (changes locally; awaiting push)
**Task summary:** Created `.claude/agents/resent-email.md` — senior lead-coordinator agent for migrating contact form email delivery from Web3Forms to Resend. Triggered by phrases like "replace web3forms with resend" / "integrate resend email". Grounded in the actual repo footprint (ContactForm.astro, contact.astro, _headers CSP, .env.example, README/deployment docs, existing api/ route conventions). Specifies: server-side-only sending via `/api/send-email` Pages Function, exact env vars `RESEND_API_KEY` + `RESEND_EMAIL_RECEIVER` (comma-separated list, parsed to array) + `RESENT_EMAIL_SENDER` (intentional RESENT_ spelling — must never be "corrected"), preservation of isSubmitting guard / CRLF strip / reCAPTCHA defer-on-intent / UI states, full Web3Forms cleanup (CSP, env, docs, zero dist references), mandatory security-reviewer final pass, verification checklist, and 9-step user setup guide.
**Files changed:**
- `.claude/agents/resent-email.md` (new)
**Next action:** none until a Resend migration is requested. Commit + push when user gives the command.

---

### 2026-06-10 (Fleet-wide agent upgrade — all 14 agents + CLAUDE.md)

**Status:** completed (changes locally; awaiting push)
**Task summary:** Upgraded the full agent fleet for quality + speed. Models: frontend-builder, ux-architect, sanity-developer → `claude-opus-4-8`; sanity-reviewer → `claude-sonnet-4-6` (was opus-4-6); seo-reviewer + security-reviewer → `claude-sonnet-4-6` (was haiku — no agent remains on haiku). Added a shared "Operating Discipline (Fast, Decisive, Zero-Mistake)" block to all 14 agents (decisive context-gathering, batched fixes, copy-from-source, no narration). Domain upgrades: a11y-reviewer WCAG 2.1→2.2 AA with the six new 2.2 criteria; seo-reviewer gained Modern Search 2026 section (AI Overviews/answer-engine readiness, E-E-A-T, entity clarity) + auto-sitemap awareness; security-reviewer fully rewritten with stack-specific knowledge (_headers/CSP, Web3Forms key is public-by-design, server-side reCAPTCHA verify, API trust boundary, OWASP lens, severity rules); frontend-builder gained mandatory LCP Implementation Rules (fetchpriority, no CSS-bg LCP, no reveal-animation on LCP, srcset mobile variant, content-visibility) + `.avif` acceptance + fetchpriority added to hero template snippet. CLAUDE.md: images line now `.webp`/`.avif`, A11y line now WCAG 2.2 AA + focus-not-obscured + drag alternatives, performance line carries LCP element rules, Modes section carries the fleet operating discipline.
**Files changed:**
- `.claude/agents/*.md` (all 14)
- `CLAUDE.md`
**Next action:** Commit + push when user gives the command.

---

### 2026-06-10 (Agent upgrades — ui-designer + performance agents)

**Status:** completed (changes locally; awaiting push)
**Task summary:** Upgraded the three core design/performance agents to "top of the world" standard. Model bumps: ui-designer `claude-opus-4-6` → `claude-opus-4-8`, performance-optimisation `claude-sonnet-4-6` → `claude-opus-4-8`, performance-reviewer `claude-haiku-4-5` → `claude-sonnet-4-6`. Content: ui-designer gained "LCP-First Design Rules" (hero image budget ≤120KB/≤60KB mobile, LCP-killer ban list, font budget, compositor-only animation rule, content-visibility note) + "Modern CSS Toolkit" (clamp, text-wrap balance, container queries, :has(), CSS scroll-driven animations, view transitions, aspect-ratio) + a new LCP block in the handoff checklist. performance-optimisation gained the Lighthouse scoring weights (TBT 30/LCP 25/CLS 25), a 4-sub-part LCP diagnosis playbook, INP/TBT + CLS playbooks, and a Cloudflare Pages playbook (_headers immutable caching, prerender verification). performance-reviewer gained an "LCP element check FIRST" section, fetchpriority + srcset checks, metric-matched font fallback check, and .avif acceptance.
**Files changed:**
- `.claude/agents/ui-designer.md`
- `.claude/agents/performance-optimisation.md`
- `.claude/agents/performance-reviewer.md`
**Next action:** Commit + push when user gives the command. Optional follow-up: mirror the LCP rules into `frontend-builder.md` so the builder enforces fetchpriority/preload at implementation time.

---

### 2026-05-23 (Tier 5 — reCAPTCHA defer-on-intent on contact page)

**Status:** completed (changes locally; awaiting push)
**Task summary:** Applied the `.claude/skills/recaptcha-defer-on-intent.md` pattern to `src/pages/contact.astro`. Replaced the static `<script src="...recaptcha/api.js">` bottom-of-page tag with a JS-injected loader that fires only on `focusin` / `pointerdown` / `submit` against form id `contact-form`. Submission flow unchanged — the existing `grecaptcha.ready()` queue handles the late-loaded script. No booking page exists yet, so the pattern was applied to contact only. No Google Map iframe exists anywhere in the base template (footer or otherwise), so the map-defer half of the request was out of scope — Fel converted that into a clone-time memory rule instead.
**Files changed:**
- `src/pages/contact.astro` — static reCAPTCHA `<script src>` → `define:vars` JS-injected loader (lines ~372–406)
- `C:\Users\felme\.claude\projects\…\memory\feedback_clone_perf_required.md` (new) — REQUIRED-on-clone memory: apply reCAPTCHA defer skill + Google Map iframe defer the moment either is added to any HMDG project clone, without asking
- `C:\Users\felme\.claude\projects\…\memory\MEMORY.md` — index entry added
**Notes:** Verified build (`npm run build`) with stub env key: `dist/client/contact/index.html` contains 0 static `<script src="…recaptcha/api.js">` tags and 6 loader tokens (`loadReCaptcha` / `focusin` / `pointerdown`). reCAPTCHA URL only appears inside the loader's `s.src = …` line. No CSS, DOM, or markup change. CSP unchanged (`www.google.com` already allowed for the script src). Performance impact: ~150KB JS + TLS handshake + scoring loop moved off LCP/TBT window; first-paint mobile PSI should rise 5–15 points on cold load.
**Next action:** Commit + push when user gives the command.

---

### 2026-05-03 (evening — Google Reviews docs + HMDG mapsUrl)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Added Section 27 "Google Reviews Carousel" to the internal docs page (between Carousels and Client Setup). Renumbered Setup 27→28 and Deploy 28→29 (badges + navLinks + comment block). Added a 6th card to the Skills Library grid for `googlereviews-carousel.md`. Bumped hero pill "5 Skills"→"6 Skills". Replaced the placeholder Harborne Chiropractic Clinic `mapsUrl` in `src/data/reviews.ts` with the HMDG public Maps URL (also dropped the now-stale `placeId`). The new docs section covers: Big Rule callout, Trustindex comparison, file layout, 5-step manual update path, review object shape, full Dynamic JSON-LD schema explainer (LocalBusiness + AggregateRating + Review[] with code preview + realistic SEO impact bullets), per-client customisation grid, optional Google Places auto-fetch path, clone steps, and a Claude AI prompt.
**Files changed:**
- `src/pages/index.astro` — new article #reviews + renumbering + Skills Library card + hero pill bump
- `src/data/reviews.ts` — `fallbackMeta.mapsUrl` → HMDG, removed stale `placeId`
**Notes:** Build clean on Astro 6.1.9 — `/index.html` prerenders in 65ms with all 9 routes successful. Verified `id="reviews"` (1) and `href="#reviews"` (2 — sidebar + mobile pill nav) in `dist/client/index.html`. Verified HMDG `mapsUrl` renders globally via `dist/client/contact/index.html` (BaseLayout includes ReviewsSection). Pre-existing stale `<!-- N — SECTION -->` HTML comment numbers in the docs file were left as-is (build-stripped, cosmetic only). Pre-existing `document.execCommand` deprecation hint at line 3498 is unchanged — out of scope per prior log entry.
**Next action:** Commit + push when user gives the command.

---

### 2026-04-30 (evening — docs Section 01 Workflow + diagrams)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Added a new Section 01 "Workflow" at the top of the internal docs page with two visual flow diagrams (Workflow A — Developers, Workflow B — Intercom Support team) plus a Roles & Responsibilities matrix and Golden Rules. Renumbered all 27 existing articles from 01-27 to 02-28. Updated 4 internal cross-references ("section 24"→"section 25", three of "section 22"→"section 23"). Two role-specific Claude prompts at the bottom (developer-start vs support-fix-ticket).
**Files changed:**
- `src/pages/index.astro` — new Section 01 + 27 renumbered articles + 4 cross-ref updates
- `.project-log.md` — this entry
**Notes:** Diagrams use HTML/Tailwind boxes with arrow connectors and inline SVG icons (GitHub mark, monitor, repo, cloud, intercom-bubble, branch, PR fork, check-circle). The "Fel merges to main" step is highlighted as the only human gate. No new dependencies. Build clean. All 28 section anchors render. Pre-existing `document.execCommand` deprecation hint at line 2860 is in the legacy clipboard fallback — out of scope.
**Next action:** Commit + push when user gives the command.

---

### 2026-04-30 (afternoon — docs page rewrite + mandatory video rule)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Rewrote the internal docs page (`src/pages/index.astro`) to match everything currently in `CLAUDE.md`, the agents, the skills, and the recent performance backport. Added 7 new article sections (Modes & Tiers, Skills Library, Performance & Build Tooling, Hero Video, Project Log Protocol, Env Variables, Cloudflare Deploy step-by-step). Added a mandatory video-format rule (self-hosted MP4 H.264 only) to CLAUDE.md and the hero-video skill. Restructured the Client Setup Checklist into Phase 1 (Marketing Inputs Required) and Phase 2 (Dev Build Steps). Renumbered existing sections 17–20 to 23–26. Removed legacy Netlify Forms script comment. Refreshed Section 16 Agents (now 12 with `performance-optimisation`, plus escalation rules and parallel/sequential waves).
**Files changed:**
- `src/pages/index.astro` — 1818 → 2894 lines (+1076 lines of docs)
- `CLAUDE.md` — new mandatory Video non-negotiable
- `.claude/skills/hero-video.md` — added MANDATORY callout at the top
- `.project-log.md` — this file
**Notes:** Wrote every new section for non-technical readability — Plain English callouts, step-by-step numbered lists, before/after comparison cards, glossary callouts. Verified all 27 section anchors render in `dist/client/index.html`. Stale TS diagnostics from the Netlify legacy script are gone (script removed). Build clean on Astro 6.1.9.
**Next action:** Commit + push when user gives the command.

---

### 2026-04-30 (morning — performance backport)

**Status:** in progress (changes locally; awaiting push)
**Task summary:** Site-wide performance backport from Northern Medical → base template. Adds Cloudflare adapter ASSETS-binding patch, dual-preload hero images, deferred GTM loader, JSON-LD `schema` prop, optional self-hosted MP4 hero video, `lang="en-GB"`, `noindex={true}` default, `media-src 'self'` CSP, 1-year cache for `/images`/`/videos`/`/icons`, ASCII-only robots.txt, `_redirects` placeholder, three resize/optimise scripts. Bumped Astro 6.1.3 → 6.1.9 with `vite ^7` override.
**Files changed:**
- `scripts/patch-cloudflare-adapter.mjs` (new)
- `scripts/resize-oversized-images.mjs` (new)
- `scripts/optimise-hero-video.mjs` (new)
- `package.json` — postinstall hook, resize/optimise scripts, sharp + ffmpeg dev deps, vite override
- `public/_headers` — `media-src 'self'`, `/icons`, `/videos`, 1-yr `/images`
- `public/_redirects` (new) — valid-status-code documentation
- `public/robots.txt` — ASCII-only
- `src/layouts/BaseLayout.astro` — lang=en-GB, noindex default true, heroImageMobile, schema prop, deferred GTM, media-src
- `src/pages/index.astro` — lang=en-GB
- `src/components/Hero.astro` — optional `video` prop, JS-injected source pattern
- `.claude/skills/hero-video.md` (new)
- `.claude/skills/performance.md` (new)
- `.project-log.md` (new — this file)
**Notes:** Build verified clean on Astro 6.1.9 + Vite 7.3.2. Smoke-test confirmed lang=en-GB, noindex, media-src, ccemail, deferred GTM all rendering correctly.
**Next action:** When user gives the push command, commit as a single feat() commit and push to origin/master.

### 2026-04-30 (earlier)

**Status:** completed (commit 9fb6a9a, pushed)
**Task summary:** Production-grade contact page using the canonical Web3Forms pattern. Inline form with the production-tested field contract (ccemail/semicolon/no `to`/`recaptcha_response`/replyto-CRLF-strip/isSubmitting guard). MedicalClinic JSON-LD. PageHero component. ContactForm.astro fixed to canonical pattern. Reviewer fixes for a11y/security/seo/conversion applied.
**Files changed:** `src/pages/contact.astro`, `src/components/PageHero.astro`, `src/components/ContactForm.astro`, `src/layouts/BaseLayout.astro`, `src/pages/thank-you.astro`, `.env.example`, `.claude/skills/contactform.md`
**Notes:** 7 files / 1,628 insertions. Skill doc captures the Web3Forms field contract and full per-client setup. See commit message for the full breakdown.
**Next action:** None for this work; superseded by the in-progress performance backport above.

---

## Session Notes

### 2026-04-30

- The Vite override (`"overrides": { "vite": "^7" }`) was added defensively — current resolution is 7.3.2, no Vite 8 in the tree, but the override prevents a future `npm install` from upgrading.
- Confirmed `BaseLayout.astro` cannot reference `import.meta.env.PUBLIC_SITE_URL` directly — Vite's static env replacement combined with const declarations there triggers misleading "Unterminated string literal" errors. Use `Astro.site` with `Astro.url.origin` fallback instead. (Documented in `.claude/skills/contactform.md`.)
- The Cloudflare adapter ASSETS-binding patch is wired via `npm postinstall` and is idempotent. Re-running reports `0 replacements, 5 already-done`.
- Astro 6.1 release brought no breaking changes. Sharp codec defaults, advanced SmartyPants, i18n fallback routes — none warranted code changes for clinic sites.
